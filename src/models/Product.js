export default class Product{

    constructor(id, name, thumbnail, price, count) {
        this.id = id;
        this.name = name;
        this.thumbnail = thumbnail;
        this.price = price;
        this.count = count;
    }
}